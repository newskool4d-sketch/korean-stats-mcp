{
  "name": "korean-stats-mcp",
  "version": "1.8.6",
  "description": "한국 국가데이터처 KOSIS OpenAPI 기반 MCP 서버 - 92개 키워드 + 자연어 별칭/오타 정규화 + 17개 시도/자치구 230+ 라우팅 + 시계열 추세 + 전국 순위(quick_rank) + 출처 각주 생성(explain_statistic) + 체인 도구 3종(지역 브리핑·다지역 비교·정책 영역) + 자치구 정밀 OpenAPI 라우팅 15종 통계표.",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "korean-stats-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "inspector": "tsc && npx @modelcontextprotocol/inspector dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "tsc && playwright test",
    "test:ui": "tsc && playwright test --ui",
    "test:headed": "tsc && playwright test --headed",
    "sim": "tsc && node scripts/simulate-realworld-100.mjs && node scripts/simulate-district-routing.mjs && node scripts/simulate-nationwide.mjs && node scripts/simulate-edge-cases.mjs && node scripts/simulate-chains.mjs",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write src/**/*.ts"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "kosis",
    "statistics",
    "korea",
    "openapi",
    "ai",
    "llm"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "dotenv": "^16.4.5",
    "express": "^4.22.2",
    "kordoc": "^4.7.2",
    "node-cache": "^5.1.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/express": "^4.17.25",
    "@types/node": "^20.11.0",
    "@typescript-eslint/eslint-plugin": "^6.19.0",
    "@typescript-eslint/parser": "^6.19.0",
    "@vercel/node": "^3.0.0",
    "eslint": "^8.56.0",
    "prettier": "^3.2.4",
    "typescript": "^5.3.3",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "pnpm": {
    "comment": "보안 패치 강제 — 전부 transitive. 각 audit 취약범위만 정확히 타겟(취약하지 않은 버전 건드리면 호환성 깨짐: 예 express 4의 path-to-regexp 0.x, ESLint의 ajv 6).",
    "overrides": {
      "path-to-regexp@>=8.0.0 <8.4.0": "^8.4.0",
      "fast-uri@<=3.1.1": "^3.1.2",
      "ajv@>=7.0.0 <8.18.0": "^8.18.0",
      "qs@>=6.7.0 <=6.15.1": "^6.15.3",
      "protobufjs@<=7.5.7": "^7.5.8",
      "adm-zip@<0.6.0": "0.6.0",
      "sharp@<0.35.0": "0.35.3",
      "body-parser@>=1.20.0 <1.20.6": "1.20.6"
    }
  }
}
